# Extended domain terminology
# Add entries as the team clarifies meanings or introduces new concepts.
# Each entry: definition (required), plus optional relationship / synonyms / notes.

entities: {}
  # example:
  # order:
  #   definition: A confirmed purchase that has completed checkout.
  #   relationship: "belongs_to user; has_many line_items"

concepts: {}
  # example:
  # idempotency_key:
  #   definition: Client-supplied token that guarantees a mutation executes at most once.
  #   notes: Required on all POST endpoints under /v2/.
