# Shared vocabulary.
# Domain terms and concepts used across the codebase. Terminology only —
# entity structure lives in ../architecture/entities.yaml.
#
# Each entry: definition (required), plus optional synonyms / notes.

terms: {}
  # example:
  # idempotency_key:
  #   definition: Client-supplied token that guarantees a mutation executes at most once.
  #   synonyms: [request_id]
  #   notes: Required on all POST endpoints under /v2/.

concepts: {}
  # example:
  # eventual_consistency:
  #   definition: Reads may reflect stale state until replication settles.
  #   notes: Applies to the read-replica pool; writes are strongly consistent.
