<!-- purpose: System shape, entry points, and module layout. Read this first to understand how the codebase is organized. -->
<!-- seam: This file catalogues modules (what exists, where it lives, what it does at the system level). Module-internal APIs — composables, plugin methods, helpers, do/don't rules — belong in design-patterns/DESIGN-PATTERNS.md under the relevant concern section. -->
<!-- glossary: Reference entities and concepts by name; canonical definitions live in glossary/glossary.yaml. Do not redefine them here. -->
# Architecture

<!-- exodia:section:intro -->
{{ONE_LINE_ARCHITECTURAL_THESIS}}

## Entry Points & Routing

<!-- exodia:section:routing -->
{{HOW_ROUTES_RESOLVE: file-based, config-based, convention-based. Cite the router file(s).}}

### Key Files

- {{LIST_KEY_ROUTING_FILES}}

## Modules & Boundaries

<!-- exodia:section:modules -->
{{CATALOGUE_MAJOR_MODULES as a table with columns `| Module | Path | Purpose | Usage docs |`. One row per module. Purpose is one line: what the module is at a system level (one sentence, under ~100 chars). Usage docs is a link to the design-patterns/DESIGN-PATTERNS.md section that owns the module's APIs (composable names, plugin methods, helpers, do/don't rules) — write `see [Authentication](../design-patterns/DESIGN-PATTERNS.md#authentication)` or similar. Do not list composables, methods, or call-site recipes here; those live in design-patterns.}}

## State Management

<!-- exodia:section:state -->
{{STATE_MODEL: stateful stores / in-memory / stateless. Where they live and conventions. Skip if N/A (e.g. pure CLI, library).}}

## Build

<!-- exodia:section:build -->
{{BUILD_TOOL: compiler, bundler, packager, or "no build step". Cite config.}}

## Runtime Model

<!-- exodia:section:runtime -->
{{RUNTIME_MODEL: e.g. server-only, client-only, SSR/CSR hybrid, batch job, daemon, consumer-runtime library. How data flows through the system.}}

## L3 Data

<!-- exodia:section:l3 -->
- `decisions.jsonl`: Architecture Decision Records for non-obvious choices.
