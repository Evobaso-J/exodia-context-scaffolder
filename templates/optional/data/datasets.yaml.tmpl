# Registered datasets used by experiments and pipelines.
# Each entry captures source, schema notes, and refresh cadence.
# Mutate by editing entries in place. This is a registry, not a log.
#
# Suggested shape, adapt as needed:
#
# datasets:
#   <dataset_name>:
#     source: "<where it comes from: DB table, S3 path, API>"
#     schema: "<one-line schema or pointer to schema file>"
#     refresh: "<cadence: daily, on-demand, weekly>"
#     notes: "<context, owner, gotchas>"

datasets: {}
