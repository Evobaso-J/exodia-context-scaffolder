{"_schema": "rb", "_version": "1.0", "_description": "Operational procedures triggered by infra events: scale, failover, cert rotation, env promotion. Append only; one runbook per entry. Mark `status: archived` when a procedure is replaced and append the new version.", "_fields": ["id", "title", "trigger", "steps", "escalation", "files", "created", "status"]}
